
# Add this to the app.gradle file and then build

signingConfigs {
        release {
            storeFile file('D:\\Workspace\\donezo\\donezo_keystore.jks')
            storePassword 'G*oG68x0*XlL#SDu'
            keyAlias 'donezo_key'
            keyPassword 'G*oG68x0*XlL#SDu'
        }
}

buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signing signingConfigs.release
        }
}